 - 포인터
  > 배열, 이중 포인터 등으로 함수를 주소 지정으로 넘기기
 - 변수
  > 전역 변수를 극한으로 줄이고 static 변수 적극 사용하기
  > 지역 변수를 최대한 이용하고 함수 끼리 공유해야 할 경우는 함수의 매개변수로 주소를 넘기기
 - 동적 할당 메모리
 - 구조체, 공용체, 열거형 등 사용자 정의 자료형
 - 파일 입출력
 - 전처리와 헤더파일

지난 번 가볍게 훑어본 개선 사항이다.
이번에 해볼 것은 변수를 최대한 지역 변수로 세팅한 후 함수의 매개변수를 적극 이용하는 것이다.
가보자!

일단, 코드의 처음인 main부터 하나씩 넘어가는 식으로 해보자

이런 작업을 하기 위해서는 코드의 진행이 어떻게 되는지 대충 정리할 필요가 있어 보인다.
기존에 함수의 내용을 정리하던 텍스트 파일에서 진행해보자

importNext함수의 기존 next출력 파트를 printUI에게 넘기고, printUI를 매개변수 값에 따라 서로 다른 UI를 갱신하도록 하였다.
기존 define으로 쓰이던 보드, 블록의 xy좌표는 일반 변수로 넘겼다.